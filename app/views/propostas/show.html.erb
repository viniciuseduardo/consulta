<h1><%= @proposta.titulo%></h1>
<% @proposta.paragrafos.each do |paragrafo| %>
	<p>
		<%= paragrafo.texto %>
		<b><%= paragrafo.comentarios.count %> Contribuições</b>
		<%= link_to "Contribuir", proposta_paragrafo_path(@proposta, paragrafo) %>
	</p>
<% end %>

<%= link_to "Voltar", root_path %>